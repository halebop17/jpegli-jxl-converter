// The output-folder picker is now part of the consolidated "Locations"
// section in InputSection.swift — this file is kept empty so XcodeGen still
// sees the directory and so any stale references resolve to a no-op type.
